# LangGraph Conversational Chatbot

Multi-session conversational AI with persistent memory using LangGraph and SQLite.

## Features
- 🔧 Integrated tools: Calculator, Stock API, Web Search
- 💾 Persistent conversation history
- 🔄 Multi-thread management
- ⚡ Real-time streaming responses

## Local Setup
1. Clone the repository
2. Install dependencies: `pip install -r requirements.txt`
3. Create `.env` file with `OPENAI_API_KEY=your_key`
4. Run: `streamlit run streamlit_frontend.py`
